const number = 125.5;
console.log(number);

const strings = 'Hola mundo'; // Tambien con comillas dobles
console.log(strings);

const boolean = true; // Tambien false
console.log(boolean);

// Strings por interpolacion
const stringInterpolada = `La variable strings es ${strings}, y el number es ${number}`;
console.log(stringInterpolada);